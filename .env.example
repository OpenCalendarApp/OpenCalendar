POSTGRES_DB=session_scheduler
POSTGRES_USER=ss_admin
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Local Node runtime (npm scripts)
DATABASE_URL=postgresql://ss_admin:change-me@localhost:5432/session_scheduler

# Container runtime (docker compose server service)
DOCKER_DATABASE_URL=postgresql://ss_admin:change-me@postgres:5432/session_scheduler

JWT_SECRET=replace-with-strong-random-secret
PORT=4000
CORS_ORIGIN=http://localhost:5173

VITE_API_URL=/api
